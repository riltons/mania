// Este arquivo é um redirecionamento para o arquivo supabase.ts na pasta lib
// para manter a compatibilidade com os imports existentes

import { supabase, supabaseUrl, supabaseAnonKey, checkSupabaseConnectivity } from '@/lib/supabase';

export { supabase, supabaseUrl, supabaseAnonKey, checkSupabaseConnectivity };